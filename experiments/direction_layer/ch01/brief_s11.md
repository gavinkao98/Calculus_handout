# §1.1 方向 brief（stage ②→③，待核可）

> 來源 seed：[`seed_s11.md`](seed_s11.md)（手稿 p.1–5）。stage ② Claude 提案、**stage ③ 人核可 2026-06-04**。
> **③ 裁決：叉路 A＝補反射圖；叉路 B＝點到為止＋forward-ref。小決定（診斷例／ID 錨／判準陳述＋一句說明／history skip）全照案。**
> 輸入：seed＋薄度剖析＋`CONTENT_ROADMAP.md` Ch1 條目（引用不複述）。核可後＝④擴寫方向依據、⑤審查 `direction-conformance` 對照。

## 薄度剖析（{夠／薄／無}）
- 函數 recall＋兩例（x, x²）：**夠**（x² 的碰撞是天然動機）。
- 反方向動機散文：**薄**。
- one-to-one 定義：**薄**（有骨架，缺圖／直覺，僅身分證號邊註）。
- 水平線檢驗＋判準：**薄**（只幾何陳述、無圖、未拆解）。
- 反函數定義＋兩例（x, x³）：**夠**。
- 互消性質：**薄**（陳述，缺直覺）。
- 三步法＋x³+2 例：**夠**。
- **無**：對 `y=x` 反射圖（標準教科書反函數的幾何核心，手稿完全沒有）；診斷型 worked example（「判斷是否一對一」）；x² 碰撞之後的收束（restrict 成 [0,∞)→√x）。

## 範圍與深度
- 只吃手稿那叢：對「全域一對一」函數談反函數、水平線檢驗、求反函數三步法。
- **水平線檢驗判準**：照手稿**陳述**，加**一句幾何說明**（高度 c 的水平線恰交於滿足 f(x)=c 的那些 x；交兩點＝兩輸入同值＝非一對一）——非形式證明，守手稿深度。
- 後章系統處理者用一行 forward-ref fence 掉：反三角（§1.2）、反函數的導數（Ch3）。

## ★方向叉路（③ 請裁）★

**叉路 A —— 對 `y=x` 反射圖：補還是不補？**（本節最大叉路，§4.2「證不證 B-W」的同型）
- 現況：手稿**完全沒有**反射圖；既有 ch01／ROADMAP key figure **有**。
- **我的建議：補**（`expansion:figure`＋intuition）。理由：(1) 它是反函數最承重的幾何直覺（「沿 y=x 鏡射＝把規則倒著走」）；(2) 低風險加法——一張 TikZ 鏡射圖，非具名結果／微妙證明，零幻覺面；(3) 對齊 ROADMAP key figure 與 Stewart/Rogawski 慣例。**與 §4.2 不同**：那是「要不要寫一段難證明」，這只是「補一張手稿漏掉的標準圖」，風險低得多。

**叉路 B —— x² 碰撞後，要不要收束到「restrict 成 [0,∞) → √x」？**
- 現況：手稿用 x² 示範非一對一（碰撞），但**不**接著說「限定域就有反函數」——把 domain-restriction 留到 §1.2（反三角）系統講。
- **我的建議：點到為止**——x² 碰撞後加 1–2 句：「若把域限到 [0,∞)，x² 變一對一、反函數為 √x」，並一行 forward-ref 指向 §1.2 的系統處理。理由：這是碰撞的自然收束、√x 是最標準反例；但不展開成一小節，避免偷走 §1.2 的主力裝置。（若你要更貼手稿，可改為全部 fence 到 §1.2、§1.1 完全不提——請裁。）

## 承重直覺
- 領頭（一個）：**反函數＝把規則倒著走；唯有「沒有兩個輸入撞到同一輸出」時才講得通**。
- 打臉用手稿自帶的碰撞：`x²` 的 `f(1/2)=f(-1/2)=1/4` ⟹ `f⁻¹(1/4)=?` 無解 → 逼出 one-to-one。（先碰撞、再形式化，符合 RULE §2 承重直覺程序。）

## worked example 清單（手稿 1 完整＋內嵌 → 提案補）
- 序：**診斷 → 建構 → 驗證**。
  1. **（新）診斷型**：判斷某函數是否一對一（水平線檢驗）——如 `x²` 在 ℝ 上不是、在 [0,∞) 上是。`expansion:example`，低風險，補手稿缺的「判別」技巧。
  2. **建構型（手稿）**：`f(x)=x³+2 → (x-2)^{1/3}`，照手稿全留。
  3. **驗證**：併在建構例的 check（互消）裡，手稿已有。
- 同型第二個求反函數例**不硬加**（手稿自帶 setup 已足；如要可加一個帶域限的，但低優先）。

## history／application
- **history：skip**（反函數無強起源故事，留白勝過 padding）。
- **application：放一個強錨**——把手稿邊註「身分證號↦學生」升格成一句真實一對一例（編碼／查表可逆），`expansion:application`。忠實的真實實例、非裝飾。

## 強調／takeaway
- 樞紐（邏輯支點）：**函數有反函數 ⟺ 它一對一**；水平線檢驗是視覺診斷。
- 可攜技能：**求反函數三步法**（寫 y=f(x)→解 x→對調）＋用合成驗證。
- 對齊 ROADMAP core skill：「determine whether a function is one-to-one, and find its inverse when it is」。

## 刻意不寫（餵 ⑤ 當反向檢查）
- **反三角函數**（§1.2）——至多一行 forward-ref。
- **domain-restriction 的系統理論**（§1.2 主場）——§1.1 只在叉路 B 點到 √x，不展開。
- 反函數的**導數／(f⁻¹)′ 與 f′ 關係**（Ch3）——forward。
- 一般 bijection／範疇語言、過度抽象的對應論——over-generalization，不漂。

## 篇幅帶
- 軟帶 **≈ 150–200 .tex 行**（補反射圖＋診斷例＋ID 錨後）；對照：既有 ch01 §1.1 ≈ 253 行（三小節）。明顯超出則回查是否某塊過擴或漏 fence。

## 記號／figures
- 記號：`f⁻¹`、域 `A`／值域 `B`、`x^{1/3}`；首用 `\index`。
- figures：水平線檢驗圖（手稿有）、**對 y=x 反射圖（叉路 A 若採納則新增）**、互消示意（手稿有箭頭圖，可轉 TikZ 或留敘述）。
