# 講義 → 影片產線 — 進度錨（現況快照）

> 本檔只保留**現況快照＋open items**（目標 ≤50 行），是跨對話進度錨、每個新 session 第一個讀的檔。
> **歷史輪次全文在 [`_archive/REBUILD_LOG-2026-05-to-07.md`](_archive/REBUILD_LOG-2026-05-to-07.md)**（2026-05-30～07-06 逐輪詳錄，含所有決策原文與 durable 教訓）。
> **維護規則（2026-07-07 制）：** 每結一輪工作＝更新本快照，該輪詳錄（決策、教訓、成本帳）寫進 archive 檔頂部；本檔不再無限 append。

## 現役路線（2026-07-07）

- **TTS**：MiMo `mimo-v2.5-tts` builtin voice **Dean**（唯一真旁白路線；Gemini/Charon 2026-06-16 退場、voice-design／Calm Professor 2026-07-05 退役）。**scene-level TTS＋forced alignment 為正式路線**：計時源＝stable-ts transcript-constrained FA（whisper-timestamped 降級 QA 探針）；`tts.py --unit auto` 涵蓋全部 content template；fallback ladder＝arbiter(small.en)→resynth→sentence-chunk→beats（chunk 受 `--fallback-budget` 自檢）。
- **文字渲染**：Route A（全 LaTeX/pdflatex；內文/標題 IBM Plex Sans、eyebrow IBM Plex Mono、數學 Latin Modern）2026-06-25 落地，Pango 路徑已移除。
- **模板／版面**：Lectern grid＋navy spine；Step 0 型階 pass＋容量契約 v2（G1–G6，advisory lint）2026-07-05 落地。
- **House audio**：Candidate B（intro/outro bed＋divider stinger gain 0.6；content 場全乾聲；caution ping OFF）。
- **審核閘**：七層閘地圖見 [`REVIEW_GATES.md`](REVIEW_GATES.md)（含 2026-07-07 gate-2 頻率矩陣）；七份 SSOT rubric 在 `content_scripts/_audit/`。
- **分支**：`main`（2026-07-19 分支統一——`video/template-redesign-navy-spine`／`video/pipeline-hardening`／`handout/latex-pilot` 全數併入 `main`，feature 分支已刪）。

## 各節狀態

| 節 | deck | 狀態 |
|---|---|---|
| ch03 §3.1 | `ch03_trig_derivatives` | ✅ **全流程完成**：clean Dean 成片 `output/ch03/s3.1/ch03_trig_derivatives_mimo.mp4`（~16.2 分、1080p）；NFA 雙閘過（gate-2 抓 1 D3 已修）；§3.1 單景 backlog 8 項打磨完（V blocking=0）。3 場 FA 降級 beats（`continuity_argument`／`derivative_of_cosine`／`shm_stacked_graphs`）＝使用者裁決「先不用」修，**勿自行重試 scene-level**（燒 billed API）。真 4K final 另議。**品質補強試點＝整節重做**（2026-07-19 定案，見 open items）。 |
| ch03 §3.2 | `ch03_chain_rule` | Stage-1 **LOCKED**（內容稿＋六鏡＋copyedit＋sign-off）；**凍結至品質補強試點 A/B 裁決**（2026-07-19；原「待 spoken／derive／NFA／storyboard render」暫停）。 |
| ch01 §1.1 | `ch01_inverse_functions` | 版面回歸 deck（舊練習產物已刪；正式 ch01 影片屆時從講義重跑）。 |

## Open items

- **▶ 下一步的開工檔＝[`KICKOFF-s31-amplify.md`](KICKOFF-s31-amplify.md)（2026-09-12 立檔，交接用）。** 上一個對話上下文將滿，未竟事項全部寫進該檔：Task A 場 08 的單位圓 hook（R2 處方，27.5 s 無圖無 marker）／Task B 原語鋪滿其餘 23 場＋全 27 場 `accent:` 依新語意軸複審＋場 20 免費重排／Task C 用新驗收線（最長靜止 ≤12 s）與 R2 重跑驗收／Task D 計費部分（R6 剩下的 06、13 beat 級改稿＋§3.1 對 `.tex` 的 §8 對齊，**先報量徵同意**）／§7 之後（模板系統設計的其餘部分：版面 4 條、數學排版 5 條、`worked_example` 模板、字體；`example_coverage_enforce`；§3.2 解凍；pdftotext poppler；7 個 commit 未 push）。該檔 §0 可整段貼進新對話當啟動提示，§1 有本輪學到的三個坑（heredoc 吃反斜線／`rewatch_pack` 靜止統計高估／selftest 全綠不代表畫面對），§2 是已驗證的 code 事實（六支原語怎麼用、驗 TTS 零成本的唯一正確方法、零行為改變的驗收方式）。

- **品質補強輪 ①「看片標記」＝改由六份模型盲審完成（2026-09-12；使用者裁決「給你來看」）。** 產物：**[`content_scripts/_audit/REVIEW-ch03_s31-rewatch-multilens.html`](content_scripts/_audit/REVIEW-ch03_s31-rewatch-multilens.html)**（逐場 sheet＋六鏡 verdict＋96 條 finding 逐條核實＋合成判定＋「每場最該改的一件事」；資料 `_gen/rewatch_multilens.digest.json`、原始輸出 `_gen/rewatch_lenses/`）。流程：`pipeline/rewatch_pack.py` 把成片翻成 pack（逐場 contact sheet＋時間軸＋靜止統計）→ 五鏡六份（R1 初學者×2、R2 動畫導演、R3 教學設計、R4 節奏剪輯、R5 講師；契約 [`REWATCH-REVIEW-RUBRIC.md`](content_scripts/_audit/REWATCH-REVIEW-RUBRIC.md)）跨 Gemini／Claude 家族盲審（agy ×3＋subagent ×3）→ Fable 逐條核實合成。**六鏡共識：內容與弧線好（R3／R5），問題集中在畫面語法——「卡片＋整塊淡入」，content 場靜止 92–97%、非 reveal 畫面變化＝0（hook 場亦然）、8 場首 beat 無 reveal（最長 37.6 s）、結論卡先於旁白；教學層只有 06（對稱論證擋在幾何前）、20（伴隨極限無動機無後續）、13（度數公式提前）被多鏡／核實指出。** 水位證據：R2 的 45 條提議多為 1–3 秒動作、只落在 5–6 種型態（原地變形／代入、聚焦壓暗、翻轉、游標掃描、跨場延續、結論卡延後）→ 支持「模板層 motion primitive」而非 per-scene hook（五個現有 hook 場靜止比例與非 hook 場無異）。鏡頭品質：R2（Opus 5）最扎實；R1a（Gemini 3.1 Pro）模板化套門檻、降權，下次初學者鏡換模型。**② 定水位＝「模板層普遍有動」（2026-09-12 使用者裁決；否決「現系統內打磨」與「每節少數招牌場」）——依據：R2 的 45 條提議多為 1–3 秒動作、只落在 5–6 種可重用原語；五個現有 hook 場靜止比例與非 hook 場無異。③ 原語清單與優先序（2026-09-12 使用者裁決）：六種原語＝1 揭示時序（拆拍／結論卡延後滑入／靜默停留 `{pause}`）、2 原地變形（TransformMatchingTex，derivation／theorem_proof）、3 游標掃描（ValueTracker，graph）、4 聚焦（`{focus}`）、5 跨場延續、6 圖跟旁白長；**首輪做 1、2、3**（4 視情況隨 2 順做；5、6 第二輪），**試點場＝04 difference_quotient（測 2）、11 squeeze_graph（測 3）、08 continuity_statement（測 1）**；06 留作第二輪驗收場。④ R6 範圍（2026-09-12 使用者裁決）＝**不重寫 Stage-1，只做三處外科修改**：06 偶函數對稱論證移到不等式之後（四鏡）、20 伴隨極限補一句用途或移到 sinθ/θ 之後（三鏡）、13 度數導數公式延後到 15 之後或 recap（R5）；每處＝post-lock 改稿 → scoped NFA → 該場重 TTS（計費，先報量）。⑤ §3.1 講義對齊（2026-09-12 使用者裁決）＝**併入本輪**：全稿對 `handout/latex/src/ch03/chapter3.tex` 做 §8 diff、逐單元判是否跟改（已知分歧：04 和差化積推導、03 開節兩句已刪），改的場一併 scoped NFA＋重 TTS（執行前先報實際場數與 call 數徵同意），完成後 `source_rev` stamp 換到 `.tex`、WARN 消失。⑥ 驗收迴圈（2026-09-12 使用者裁決）＝離線工程 → 機器閘（既有閘＋visual-frame-audit＋**R2 導演鏡以 Opus 5 subagent 重跑 before/after**；驗收線：① 級關閉、verdict ≥ ok、無新 must、靜止 <70%）→ **唯一人閘＝三場真旁白短片給使用者看**（`--reuse-audio`，不重 TTS）→ 放大。**Kickoff＝[`KICKOFF-motion-primitives.md`](KICKOFF-motion-primitives.md)（使用者指示：用新對話開工，中途不停）。** 人類標記表 [`REVIEW-ch03_s31-rewatch-marking.html`](content_scripts/_audit/REVIEW-ch03_s31-rewatch-marking.html) 時間戳已校正、保留為人類通道。REWATCH 是否常設為閘另議。
- **品質補強輪 ⑦＝首輪三原語落地＋三場試點（2026-09-12；kickoff [`KICKOFF-motion-primitives.md`](KICKOFF-motion-primitives.md)）。** **全程零計費**（mock render＋現有 Dean 音檔 reuse；`tts.py` 收據 `backend_calls: 0`，旁白一個字未改）。 **落地的三支原語（全部 opt-in，契約寫進 [`DESIGN.md`](DESIGN.md)「motion primitive」節）：** ① 揭示時序——`say` 寫了 `{show statement}` 時 `theorem_proof`／`derivation` 的字卡才改為該拍滑入、`{show proof.0}` 時 `PROOF` 小標跟第一行證明一起進場（以前 reveal 打在 static block 上＝對已在畫面的 mobject 再播一次 FadeIn，前後兩幀無差）； ② `anim: transform`（`derivation` 的 `steps[i]`／`result`）——上一列原地變形成這一列、來源列退為 muted 0.55； ③ `kind: sweep`（`graph` 的 `plots[]`）——`ValueTracker` 驅動游標＋曲線掛點＋兩線間色帶。附帶修好 `dashed: true` 對 `kind: function` 失效。 **兩處設計偏離 kickoff（理由已寫進 code 註解與 DESIGN）：** (a) `pauses:` 不改 `scene.py`／不在 compose 切接——改為一支純旁白變換 [`pipeline/pauses.py`](pipeline/pauses.py)：在該 beat 音訊起點插靜音、同拍時長加同樣秒數，於是 render／compose／`timeline.json`／`.vtt` 全讀同一份「已加停頓」的記憶體 manifest，不可能各自漂移；磁碟 `manifest.json` 不改寫（reuse／freshness 對它雜湊）。 (b) transform 用 `TransformMatchingShapes` 而非 kickoff 建議的 `TransformMatchingTex`＋`substrings_to_isolate`——實測後者把 `rac` 從引數切開會直接 LaTeX 編譯失敗，且會擾動被切列的間距；字形配對不改 mob 建法，終態幾何與未變形版逐 token 相同。 **未做（連同理由）：** P2-4（`theorem_proof` 的 `proof[]` 支援 `anim: transform`）——`proof:` 的元素型別從字串改成物件會牽動 lint／pedagogy／coverage／provenance 四個讀它的稽核模組，而三場試點沒有一場用得到，不值得為零收益改契約；P3-4（`tangent_on`）kickoff 本就標「可選」。 **驗收數字（`rewatch_pack` 4 fps 差分；before＝2026-07-05 成片）：** 11 `squeeze_graph` 靜止 94%→**72%**、最長靜止 20.2 s→**7.2 s**；04 `difference_quotient_for_sine` 97%→94%；08 `continuity_statement_sin_limit` 96%→96%。 **R2 導演鏡 Opus 5 subagent 盲審重跑（只讀 pack）：** 11 `ok`→**`good`**（原 must 與 nice 全關）、04 `weak`→**`ok`**（原 must 仍在，一條 should 關閉）、08 `weak`→`weak`（原 must 仍在）；全片 must 3→2、無新 must。 **本輪的核心發現（放大階段的依據）：** 三支原語只有 **sweep 達標**，因為它是唯一能**跟著一整拍長**的原語（在 11 佔滿 8.1 s 的那一拍）；transform 一次 1.2 s、字卡滑入 0.5 s，放進 20–27 s 的長拍裡動不了水位。 R2 未關的兩條 must 也都指向本輪範圍外的原語——04 要的是把 $h=2\cdot\tfrac h2$ 拆成三段編舞（原語 4 聚焦），08 要的是把單位圓搬回畫面（原語 5／6），正是 kickoff §6 自己預告「本輪不做」的那 27 秒。 **且揭示時序修好之後，08 的前 27 秒比以前更空**（舊版至少有一張提前劇透的字卡佔版面）——此取捨待使用者裁決。 另：`pauses` 在靜止比例上是倒扣的（它刻意增加靜止），若續用該指標當水位，建議把 `pauses` 宣告秒數從分母扣掉。 **零行為改變證據：** 18 個 deck 的 schema／lint／sizecheck 報表逐字相同；`run_selftests.py` 23→**27 全綠**（新增 `_selftest_reveal_timing`／`_selftest_pauses`／`_selftest_transform`／`_selftest_sweep`）；`doctor --smoke` video-smoke 9/9。 marker 驅動的原語會在另外 12 場自己寫了 `{show statement}`／`{show proof.0}` 的場一併生效（ch01 1 場、ch03_chain_rule 5 場、ch03 §3.1 另 4 場、`_demo_*` 3 場）——皆為該場作者本就宣告的意圖，幾何與閘報表不變。 **交付物＝[`content_scripts/_audit/REVIEW-ch03_s31-pilot-ab.html`](content_scripts/_audit/REVIEW-ch03_s31-pilot-ab.html)（三場逐場前後對照＋驗收線逐條核對）＋三場真旁白 1080p 短片。狀態＝等使用者看片裁決是否放大。**
- **品質補強輪 ⑧＝驗收線改訂「靜止比例 → 最長靜止秒」＋「模板不是病灶」的證據（2026-09-12 使用者裁決）。** 起因＝使用者問「模板是不是太死板」。對 `_gen/rewatch_multilens.digest.json`（六鏡 ×27 場，96 條 finding）跑相關性，結論與直覺相反，**三項證據都指向「病灶不在模板層」**： **(a) hook 與模板對品質的解釋力≈0。** `corr(verdict, hook) = −0.11`；純模板 content 場（n=16）平均 verdict **2.36**、hook 場（n=5）**2.27**——掙脫模板的場還略低。**(b) 全片最差的一場正是最自由的那一場**：`sector_inequality`（手寫 Manim hook）verdict **1.17**（全片最低）、最長靜止 **38.2 s**（全片最長）；R5 判它的病是「開場 38 s 講負角奇偶對稱、學生還沒看到那三塊圖形」＝**Stage-1 旁白編排問題，換模板或寫 hook 都治不好**。hook 場 verdict 從 1.17 橫跨到 2.83（`slope_equals_height`，三鏡點名最強）**幾乎撐滿值域**，說明 hook/模板這條軸不是驅動變數（n=5，不宜過度解讀均值，但值域這件事成立）。**(c) 模板該負責的那格幾乎沒被抱怨**：96 條 finding 裡 `D-composition` 只有 **5 條且 0 條 must**；24 條 must 的分布是 **L-attention 13**、D-carry 4、L-why 2、T-dwell 系 3、L-follow 1、D-motion 1。 **驗收線改訂（本輪核心裁決）：** content 場 verdict 的預測因子排序＝`longest_still_seconds` **−0.82** ≫ `秒/reveal` −0.60 ＞ `場長` −0.58 ＞ `reveal 數` +0.30 ＞ **`static_ratio` −0.18**（幾乎無關）＞ `hook` −0.11。**本輪訂的「靜止比例 < 70%」是在優化一個與品質無關的數字**，而本輪 A/B 自己就是反證：11 升 `good` 靠最長靜止 20.2→**7.2 s**，04 靜止比例改善 3 點但最長靜止 **+0.8 s**、must 因此沒關。**新閘＝每個 content 場 `longest_still_seconds` ≤ 12 s，超過為 finding 但可一句理由豁免（warn-default，比照 lint／sizecheck）**；門檻取法＝21 個 content 場掃描，12 s 命中 10 個好場（verdict ≥ 2.5）中的 6 個且**誤收 0 個差場**，14 s 命中 8 個但開始誤收（F1 最佳 0.84 卻 precision 破功），**閘寧可誤報不可漏放故取 12 s**。`static_ratio` 降為只記錄不設門檻（且引用時要扣掉 `pauses:` 宣告秒數）。已改 [`KICKOFF-motion-primitives.md`](KICKOFF-motion-primitives.md) §7 第 1 條（§1 原成功定義保留原文並標註被推翻，因它是本輪被判定時的依據）。 **順帶查到的兩個「有能力沒用」缺口（皆非模板限制）：** `theorem_proof`／`definition_math` 早就支援 `aside:` 右 rail，§3.1 這 27 場**用了 0 次**——R2 的「版面只用左半 55–65%、右側長期留白」不是模板逼的；`derivation`／`theorem_proof` 承載 27 場中的 11 場、預設 reveal 是整行淡入，而 `anim: transform` 已證明模板可以承載動作、不必被取代。 **方向裁決：否決「鬆綁模板／多寫 hook」**（數據不支持），改為 ① 換驗收指標（本條）② 把已有的 `aside`／`transform`／`sweep`／`pauses` 用起來 ③ **下一支原語要能填滿長拍**——這正是新指標會獎勵的東西（`sweep` 是目前唯一能跟著一整拍長的原語）。
- **品質補強輪 ⑩＝影片內容取捨的三件事：分工升格、幕封裝（否決）、例題折疊宣告閘（2026-09-12；四鏡＋終審收斂後執行）。** 起因＝使用者問「影片內容取捨是不是也要重新設計——講義只有重點會進影片，不然影片太冗長」與「要不要讓講義指導影片，還是把講義當大綱、兩條線分開」。**流程：** 提案 → 四鏡獨立並行裁決（教學設計 Opus／產線工程 Opus／契約一致性 Sonnet／反對 Sonnet）→ Fable 5.1 終審親驗收斂 → 執行 → 回歸審核（Opus）。定稿＝[`PROPOSAL-scope-packaging-coverage.md`](PROPOSAL-scope-packaging-coverage.md)（含四鏡裁決、未決點逐條表態、執行順序、驗收、已知風險）。 **① 分工升格＝做（純文檔）。** 講義治涵蓋／正確性／可回溯，影片治順序／切分／節奏／畫面構成／視覺發明；兩條操作規則「重排不需要理由、漏掉才需要」「重排不得違反數學依賴序」；範圍限**新稿的場級**順序（已鎖稿內容的 beat 級走 §8）。**原提案的論證被契約鏡與反對鏡各自獨立推翻並已改寫**——「順序自由沒升格成通則」是**錯的**，`CONTENT_METHODOLOGY.md:34` §1 與 `:82` §3（整節＋12 步非書序偏好流程）從檔案誕生第一天（`10acfd5c`，2026-06-01）就有，比 §3.1 內容稿 LOCKED（2026-06-29）早近一個月；真正的現況是「規則在、撰稿時沒遵守、鎖稿前沒自檢」，故藥方改為「升格＋§7 順序自檢」而非「補真空」。**U1-a 依賴序閘＝四鏡一致否決**（依賴已被 narration 的回指句表面化、違序會產生無先行詞的句子＝gate-1／R3 `A-bridge` 看得見；步驟級已有 `screen_contract.required_steps[].depends_on`；零實際案例；`depends_on` 欄位名已被佔用，日後若做用 `order_depends_on`）。**②「一支影片＝一幕」＝不做。** 三個否決理由：(a) 與 `CONTENT_METHODOLOGY.md:40`「一節 = 一支影片（小節之間不另做過場片）」MUST 規則**正面對撞**而原提案未提（契約鏡與反對鏡各自獨立找到同一行）；(b) 前提「16 分鐘太長」**查無稽核證據**——六鏡 96 條 finding 與品質補強輪 ①–⑨ 沒有一條講片長，五鏡抱怨的是畫面不動；(c) 四幕**不是獨立單元**——三個幕首的先行詞都在幕外（幕 2「Here is that new tool」→ 場 03、幕 3「we already did the hard algebra: that quotient is…」→ 場 04 第 1 幕、幕 4「The fundamental limit has a companion」→ 場 10），第 3 幕六鏡零 weak 不是因為它 2.6 分而是因為前兩幕已把帳付完。**終審抓到四鏡全部漏看的關鍵事實：節內導航早就做完了**——`make.py:771-773` 每次 compose 都寫 `<stem>.chapters.txt`（`captions.to_chapters`：intro tagline 在 0:00＋每個 divider 一個跳轉點、YouTube 章節格式；T7 2026-07-11 落地、`DESIGN.md:1059-1061` 有記）。教學鏡提的替代方案 N1 就是它。**`:40` 原句逐字未改，只加一句 chapters.txt 指標。唯一重開條件＝使用者看過帶章節標記的成片後仍要獨立檔**（屆時前置＝先改 `:40` 規則＋先裁場 20 位置；技術降規結論已存進定稿 §3 末段）。 **③ 例題折疊宣告閘＝做，但技術設計被工程鏡推翻重寫。** 原提案兩個技術前提**一個已成立不需做、一個做不到**：(a)「`provenance.py` 要加 `doc:ex:`」是錯的——`provenance.py:53` 的 `_TEX_ENV_KEY` 是泛型 `[a-z]+:`，`ex:3.1`–`ex:3.16` 今天就解得出來，成本 0 行；(b) 涵蓋判定靠 storyboard 的 `ref:`／`refs:` **在既有文法下不可能**——全 18 個 storyboard 零個 `doc:<kind>:` ref、教學場 `ref:` 全被 `md:<unit>` 佔滿、`refs:` 是 per-field OF1/OF2 覆寫借來會污染 provenance 閘；照原稿實作 §3.1 三題一個都不命中、與自訂驗收直接矛盾。**改為：宣告全部進 `.md` 代表單元（`examples:`／`folds:`），閘純 `.md` ↔ `.tex` 比對、不讀 storyboard**（於是③與①互不干擾：重排 storyboard 不影響涵蓋）。**另一個災難性陷阱：`ex:` 編號是章序不是節序**——§3.1＝`ex:3.1–3.3`、§3.2＝`ex:3.4–3.8`、ch01 六節連號跑 `ex:1.1–1.43`，**`ex:3.2` 不在 §3.2**；歸節必須按 `\sechead` 區間掃描、集合比對（`calcbook.sty:271-276` 明文：label key 沿用歷史號、不承諾等於印值）。 **落地物：** 新 [`pipeline/example_coverage.py`](pipeline/example_coverage.py)（EX1 silent drop／EX2 宣告不成立；warn-default，`meta.example_coverage_enforce` 才 gating；**deck 無 `.md` 整個跳過**）＋`review_pack._FIELD_KEYS` 註冊 `examples`／`folds`＋`_commit()` 對 `folds` 保留原始行（不然多筆折疊被空白 join、第二筆 key 讀成第一筆理由）＋只接 `schema.py:main()`（**不接 `make.py`**，同 SC 先例：例題選材是撰稿期決定、不擋 render）＋`_selftest_example_coverage.py`（15 test，含章序陷阱、`_FIELD_KEYS` 漏註冊、`examples_for_deck` 靜默失效）。 **閘有效性實證：回填前對三個正典 deck 準確吐出 3／3／5 個 EX1**（§3.1 的 `ex:3.1–3.3`、§3.2 的 `ex:3.4–3.8`，一題不漏一題不多），回填 8 行 `examples:` 後全部歸零。 **執行中修掉三個定稿未預見的問題：** (i) `ch01_inverse_functions` 無 `.md`（gen-1 遺留）卻被咬 8 個 EX1——定稿寫了「無 `.md` 則 skip」漏實作，`doctor --smoke` 比對抓到（44→52 WARN），修完逐字相同；(ii) `folds:` 多行被 parser 空白 join（正是定稿風險清單裡「理由含 `ex:` 被讀成下一筆」的根因，改為保留行後該風險**從源頭消失**）；(iii) 裸 `ex:3.3`（缺理由）原落進「格式錯誤」，改為解析成「折疊但缺理由」＝診斷更準。 **驗收（回歸審核鏡 Opus 獨立複驗）：** `run_selftests` 28→**29 全綠**；三個正典 deck `[example_coverage]` 零輸出且 `enforce=True` 下仍 0；`doctor --smoke` 與改動前**逐字相同**（另以 detached worktree 取 HEAD 基線，schema×4／lint×4／derive --check **9/9 byte-identical**）；`derive_spoken --check` parity OK；`build_packet_text` 對全部 7 份 content script **sha 未變**（`_FIELD_KEYS` 加欄位不影響 engineering packet）；`\sechead` 歸節對 ch01＋ch03 共 59 題全部歸節零掉落；8/8 回填逐題比對講義原文全對；兩份 `.md` diff 只有 `+examples:` 行、narration 與 `source_rev` 標頭零改動。回歸審核 **0 blocking**；其 F1（本條 `REBUILD_STATUS` 未更新）＋F2（`examples_for_deck` 無測試＝全模組唯一靜默失效點）＋過度設計（`_split_keys` 的 list 分支是死路）已於同輪修畢。 **交付物＝[`content_scripts/_audit/REVIEW-example-coverage-applied.html`](content_scripts/_audit/REVIEW-example-coverage-applied.html)**（四鏡裁決摘要＋收斂路徑＋回填前後閘輸出＋回歸審核結果）。 **未做（連同理由）：** 不壓縮內容（六鏡無一說「講太多」）；不拆講義與影片的內容權威（那層耦合擋的是講錯講漏，而使用者要的自由本來就沒被任何閘綁住）；**本輪不開 `meta.example_coverage_enforce`**（warn-default 是所有閘的落地慣例；開 enforce 要動正典 yml meta → `_mimo` 需重 derive，留給 §3.1 整節重做輪順做）；`ch01_inverse_functions` 不回填（無 `.md`）。
- **品質補強輪 ⑨＝模板系統重新設計的第一步：語意色軸對位講義（Direction B；2026-09-12 使用者裁決）。** 起因＝⑧ 之後使用者問「模板當初設計沒特別考慮，要不要用 Claude Design 重新設計一次（視覺／數學排版／版面佔比／應用）」，並同意「可以打掉重用」、要求先看過定稿講義再給決定性意見。 **依據＝全書內容型別盤點**（掃 `handout/latex/src/` 全部 12 章／附錄、66 節、124 小節，數 `\begin{env}`）：全書 **935 個語意塊**，現有 12 個模板**完整覆蓋 361（39%）、擠壓或部分 248（27%）、完全無模板 302（32%）**。 **三個發現：(a) `workedexample` 220 個（佔 24%）是全書單一最大宗，而影片產線沒有對應模板**——§3.1 的 27 場一個例題場都沒有，不是那節沒例題（有 16 個），是沒模板可放；**(b) 章與章體質差極大**（ch04＝14 定理／19 證明、ch01＝43 例題、ch07＝23 圖），一套固定版面比例服務不了三種；**(c) 兩條線的語意色是對調的**——講義 `calcbook.sty` 說 definition＝赭 `aConcept`／theorem＝藍 `aResult`，影片卻是 definition＝藍／theorem＝琥珀，且 caution／strategy／remark 三種語意擠在一個 `callout`（共 140 個單元）。 **交付物＝設計畫布 [影片模板系統](https://claude.ai/code/artifact/b6bbb677-135e-48fc-8d0d-9ff13cca05ca)**（3 頁 9 artboard：覆蓋矩陣／新模板集合／三識別方向／版面構成規則／數學排版規則／`worked_example` 新模板 mockup；`.dc.html` 工作檔目前在 scratchpad，未進版控）。 **模板集合提案＝13 個而非 20 個**：關鍵是把「語意」從模板抽出來當參數（caution／strategy／remark 是同一骨架配不同語意色與容器形狀），list／matrix／piecewise／informal（共 82 個）在講義裡幾乎都巢狀在別的環境內部，故為**區塊原語**不是模板；覆蓋率 39%→89%（其餘 11% 由原語吃）。 **三個識別方向的軸＝「影片與講義的關係」**：A 同語（直接用講義視覺文法）／B 對位（深色舞台＋語意色一一對位）／C 分工（語意改由容器形狀＋版位編碼、顏色只留 2 個讓給動畫）。**使用者裁決＝B，並要求落地到 `theme.py` 與模板層。** **本輪落地（本 commit）：** `theme.py` 加六個語意 role（`concept`／`result`／`practice`／`caution`／`strategy`／`aside`）＋ink tint，**LIGHT 逐字沿用講義 hex、DARK 保色相提亮**（契約與對照表見 [`DESIGN.md`](DESIGN.md) §語意色軸）；`blocks.ACCENT_ROLE` 重映射到講義軸（definition 與 theorem 對調回來）；`DEFAULT_ROLE` 由「退回 definition」改為中性 slate（definition 現在是帶標記的赭，再繼承等於替作者宣告他沒寫的語意）；修掉 4 個模板寫死 `accent` 的地方（`theorem_proof` statement 卡色條 ×3、`derivation` result 行與 leader、`recap_cards` 序號、`definition_math` 的 `anim: highlight` 行）——`intro`／`outro` 的 `role="accent"` 是淺色品牌幀的品牌紅、**不是場語意，刻意不動**。 **新閘 [`_selftest_semantic_palette.py`](pipeline/_selftest_semantic_palette.py) 重讀 `calcbook.sty` 比對色相**（DARK 容差 4°），任一條產線動了自己的色軸即紅，兩線不會再靜默漂開。 **驗收：** selftest 27→**28 全綠**；18 個 deck 的 sizecheck **失敗集合改動前後完全相同**（`_demo_aside`／`_demo_capacity`／`_demo_multipage`／`_demo_tall_rows` 四個刻意的容量壓測 fixture，設計上就該紅）；四場 mock render 逐幀看過。 **明確未做（連同理由）：** ① **字體未動**——B 的 mockup 用 Instrument Sans，但落地要裝 LaTeX 字型（CLAUDE.md 須先徵詢）且 `brand.py` 的 `_WIDTH_K = 0.00507` 是對 Plex 校準的、換字得重測；B 的核心價值是色彩對位不是換臉。② **版面構成與數學排版兩份規範未落地**（畫布 page-3：結論必須最重、下三分之一不得空置、右欄條件展開、三種佔比；數學行內不得混散文、rail 只有兩種語域、正斜體、∎ 是字形不是元件、數學字級收成三階並取消 `math_sm 40`）——其中取消 `math_sm` 會動到現有 deck，待裁決。③ **`worked_example` 模板未實作**（最大缺口，220 個單元）。④ **全 18 個 deck 的 `accent:` 值未依新軸複審**——已知 §3.1 的 `difference_quotient_for_sine` 標 `accent: definition` 而整場赭色，但它推導出的是 result，語意上該是藍；此為 storyboard 的值不是 code，未擅改。
- **品質補強輪 ⑪⑫（2026-09-12）的紀錄只在 commit body**——`30137e7`（原語 4 聚焦／5 跨場延續／6 圖跟旁白長）與 `ad03732`（場 06 beat 0 拆階段＋偶函數對稱動畫）。契約已於 ⑬ 補進 [`DESIGN.md`](DESIGN.md)「motion primitive：`focus:`／`color_role`／`seconds: beat`」節。

- **品質補強輪 ⑯＝畫面語法四原語鋪滿 §3.1 27 場（2026-09-13；kickoff [`KICKOFF-motion-language-rollout.md`](KICKOFF-motion-language-rollout.md)，使用者「鋪滿 27 場吧」）。** 先修三個放大階段會咬到的 code 缺口（兩個 worktree 代理並行）：T1 transform／cancel 列在 `paced:` 裡時 morph 後把 rail（leader／reason）鋪在拍子剩餘時間（`pacing.walk`）、callable 掛 `fixed_seconds` 讓 `[stillness]` 對 transform／cancel／carry 誠實、theorem_proof 的 `proof[]` 收 `{tex, anim: transform, frame}` dict 列（`proof_texts` helper、schema 擴檢）；T2 列級 `seg_roles`（`{{}}` 一段一色、段色蓋 token、schema 三條 error）＋06 hook 的 `ineq` 三色、`lB` 離弧、三標籤底墊。**鋪法（21 個 content 場，旁白零字未改）：** 規則 1 carry 11→12（主圖飛右上）、23→24（`s''=−s` 飛左下）＋06／11／23 `exit`；規則 2 07／14／21／22／23 的 derivation 列與 10／16／17 的 proof 列改 transform＋`{{}}`（13 列）；規則 3 十場 `focus indicate`＋24 `dim`；規則 4 補 paced 六場；規則 5 07 三列 `seg_roles` 繼承 06 三塊區域色（取倒數＝顏色左右對調）。04 的 carry 因右上無空位放棄。**零計費：** 21 場口語 hash 逐字相同；12／24 新 marker 重對映收據 `backend_calls: 0`——**24 是 beats 模式，beat 級 reuse 以輸出檔路徑為 key，reveal 進了檔名，加 marker 要先把 WAV 依 reveal 改名（`01_carried_shm.wav`）manifest 才命中**。**驗收：** selftest 40/40、smoke 逐字相同、schema／sizecheck 0 error；三次 1080p render（首渲染 24 攜帶列在右下壓到品牌 motif——sizecheck 不查 decoration 層，抽幀才看見→改 `bottom_left`；視覺閘 1 blocking＝08 proof 列走 prose 路徑不吃色表、θ 白 vs 圖赭→改純 math 列＋`\text{}`→回歸 0）；`rewatch_pack_after16` 21 場 fine 最長靜止**全 ≤ 11.5 s**（14 場比 ⑬ 更短）；R2 Opus 盲審 27 場 **good 9／ok 13／weak 5／bad 0**、must 2（09 死區、23 彈簧沒畫＝內容提案）、`by_rule` ML1 3／ML2 4／ML3 1／ML4 7／ML5 3；試點四場全部 good。**順帶修：** carry 角落水平錨改內容 gutter（`70efe7d`，成片渲染於其前）。**與另一 session 並行（2026-09-13 教訓）：** 同一工作樹兩個 session 同時改 `derivation.py`／`graph.py`／hooks／storyboard，靠 ListAgents 找到對方、用 SendMessage 分工（它只 stage 自己的 hunk 先 commit、之後改在 worktree `s31-taskd`；我 render 期間它不跑 TTS）；**規矩＝不動對方 dirty 的 hunk、只 commit 自己的路徑、render 與 tts 的時間窗互相通知**。交付物＝`output/ch03/s3.1/ch03_trig_derivatives_mimo__rollout16.mp4`（42.5 MiB，只在桌面 app 可見）＋[`REVIEW-ch03_s31-motion-language-rollout.html`](content_scripts/_audit/REVIEW-ch03_s31-motion-language-rollout.html)（§零 逐場落點／靜止前後／場末幀＋R2 digest）；backlog 在 kickoff §7（色軸跨場打架要使用者裁決；prose 行內 θ 的 deck 級修法；角落複本可讀性；09／23 的圖）。**狀態＝等使用者看全片；另一 session 正做 Task D（計費改稿）＋09／12 hook，之後六鏡再審＋新舊 A/B。**

- **品質補強輪 ⑮＝畫面語法四原語落地＋§3.1 四場試點（2026-09-13；kickoff [`KICKOFF-motion-language-gaps.md`](KICKOFF-motion-language-gaps.md)，使用者「繼續做下一輪」）。** 三個 worktree 子代理並行實作後合併：T1 `meta.color_map` deck 級變數色表（[`pipeline/texparts.py`](pipeline/texparts.py) macro-aware 分詞、token 級上色；**不用** manim `tex_to_color_map`——它是 raw 子字串配對，`h` 會切到 `\theta`／`\right`）、T3 `{{}}` 分段＋`TransformMatchingTex` 對位＋`anim: cancel` 兩段式消去＋`frame: true`、T2 `focus[].indicate`＋graph `inset:`（主圖不縮放、follow sweep）、T4 `carry:`（靠版面決定性重建前場 block、`Block.pre_play` 開播前退回原位、場界零淡黑、`block` 可帶一組）＋`exit:`；T5 三門檻對齊（R4 `T-still` 15→12 s）。契約全在 [`DESIGN.md`](DESIGN.md) 四節。**全程零計費**（reuse 重對映收據 `backend_calls: 0`）；selftest 33→**38 全綠**、doctor smoke 9/9、三個非試點 deck 閘報表逐字相同。**試點（04 色表／06 閃爍／07 攜帶＋分段變形／11 放大鏡）：** 視覺閘首輪 1 blocking（06 hook 自建標籤繞過色表，圖上 θ 白、式子 θ 赭）→ 三個標籤改走 `brand.math_line` 後回歸 **0 blocking**；R2 導演鏡（Opus 5 盲審）首輪 must 2（06 主圖太小 beat 3–4 無變化、11 鏡片把停在 0.02 的實心點放大成蓋住空心圈）→ 11 `x_to` 0.02→0.12、carry 改帶 `[circle, frame, apex]` 一組後重跑 **06 good／11 good／04 ok／07 ok、must 1**（04 的 `h` 無色＝hook lane，`h` 因此暫不進色表）；`rewatch_pack_after14`（0.05%）最長靜止 04 9.0／06 9.0／07 11.2／11 7.2 s 全 ≤ 12 s。**三個教訓：** ① transform 1.2 s 填不滿長拍——改成 transform 的列會失去 `paced`（callable 免檢），07 首跑 result 拍靜止 15 s 而 `[stillness]` 沒抓到，改回 step.1 paced、只讓 result 變形；② hook 自建 MathTex 不吃色表、inset 鏡不到 hook 幾何——hook 內文字必須走 `brand.*`（待寫進 METHODOLOGY §5）；③ 場景沒宣告 `accent` 時 Indicate 落到 `aside_ink` 灰色像變暗（已改 `amber_ink`）。交付物＝四場 1080p 短片 `output/ch03/s3.1/ch03_trig_derivatives_mimo__difference_quotient_for_sine_sector_inequality_squeeze_to_the_bound_squeeze_graph.mp4`＋[`REVIEW-ch03_s31-motion-language-pilot.html`](content_scripts/_audit/REVIEW-ch03_s31-motion-language-pilot.html)（R2 digest 含 `by_rule`）；剩餘 advisory／should 列在 kickoff §8。**狀態＝等使用者看片裁決是否把四原語鋪滿 27 場。**

- **品質補強輪 ⑭＝替代方案研究＋參考影片逐幀拆解→畫面語法五條規則進規格（2026-09-12～13；使用者裁決）。** 起因＝使用者問「現有架構之外還有什麼方案」與「想跟 YouTube 精緻頻道看齊」。研究：五個家族（換引擎／narrated slides／agent 起草／生成式影片與虛擬講者／真人）→ 使用者收斂到 A（換引擎）或 C（agent 起草）；再用 agy（Gemini）＋WebSearch＋內建瀏覽器找候選並逐條開連結核對，使用者選 A1 3B1B ch3、A2 Think Twice、B1 Ghrist Lecture 10、C1 Morphocular 四支。四個子代理各自 headless-Chrome 抓幀（不下載）、挑 8 個時刻、寫分析，主代理逐組核對動作條後合成 [`content_scripts/_audit/REVIEW-reference-videos-A1A2B1C1.html`](content_scripts/_audit/REVIEW-reference-videos-A1A2B1C1.html)（32 張對照卡＋我方 §3.1 幀）。**結論：精緻不來自引擎（A1／C1 Manim、A2 PowerPoint 或 AE、B1 PowerPoint 做出同一套語法）、來自五條畫面語法規則；規格＝[`SPEC-motion-language.md`](SPEC-motion-language.md)（一場一張畫布／畫出來只動變的 token／框放大鏡調暗不靠鏡頭／靜止是設計的／語意色貫穿圖與式），逐條對位既有原語 1–7、`focus:`／`color_role`／`paced:`，並列出缺口與驗收信號。** 工具進 [`experiments/reference_frames/`](experiments/reference_frames/)（幀不進版控，`analysis.json` 帶秒數可重抓）。附帶：agy `--mode plan` 會在 add-dir 內寫檔執行、搜尋結果 ID 與標題配對不可信——已修根 CLAUDE.md。 **同日接線（使用者「按照建議一次做完」）：** ① 規則 4 advisory＝`make.py` `[stillness]`（`pipeline/stillness.py` 純函式＋`_selftest_stillness.py`；6 s、warn-only；正典 deck 實跑抓到 04 的 beat 02／03 各 6.9／6.4 s，`pauses:` 那拍正確豁免）；② 規則 5 視覺閘＝VISUAL-FRAME **V10 語意色一致**（agent 提示／REVIEW_GATES／`critic.py` V 範圍同步 V1–V10）；③ REWATCH finding 可標規則代號 `rule: ML1`–`ML5`（rubric `rule` 小節逐字注入每鏡 prompt、schema enum、merge `by_rule`、gen 小表；舊 digest 回歸 diff 只多小表）；④ 攜帶宣告存在性依附未建的 `carry:` 欄位，併入 **[`KICKOFF-motion-language-gaps.md`](KICKOFF-motion-language-gaps.md)**（T1 變數色表／T2 inset＋框選＋閃爍／T3 token 級變形＋兩段式消去／T4 跨場攜帶＝靠版面決定性重建前場 block、場界零淡黑、不需序列化座標／T5 三門檻 6 s／12 s／R4 對齊；§2 是兩個探索代理逐行核過的 code 事實）。

- **品質補強輪 ⑬＝原語 7「逐段隨讀」＋把原語鋪滿 §3.1 全片＋驗收（2026-09-13；kickoff [`KICKOFF-s31-amplify.md`](KICKOFF-s31-amplify.md)）。** **成果：21 個 content 場的 `longest_still_seconds` 全部 ≤ 12 s**（⑧ 訂的驗收線；0.05% 門檻、真 Dean 音檔量測），最大改善 06 `sector_inequality` 38.2→10.0、08 `continuity_statement_sin_limit` 27.2→10.8、24 `shm_stacked_graphs` 23.8→11.8、13 `radians_essential` 21.0→11.0、04 `difference_quotient_for_sine` 20.2→9.2、03 `why_trig_is_different` 18.2→8.5。**旁白一個字未改**（27 場 `scene_text_hash` 逐字相同）。 **新原語 7＝場級 `paced: [<reveal id>…]`（[`pipeline/pacing.py`](pipeline/pacing.py)）：** 多段的 block 逐段淡入、平均分佈在整拍，**切 n 個間隔而不是 n−1**（只切段間的話兩段的 block 會在前半拍放完、後半拍整片靜止＝把病灶搬家）；沒有段可走的單行式子改為「隨旁白書寫」（`Write` 跨整拍、速率有上限）。接在 `build_blocks` 的 hook 之後，**只升級 stock reveal**（anim 已是 callable 的 hook／`anim: transform` 一律跳過，否則通用走法會把那支編舞靜靜吃掉）。 **原語 1 補兩個 ⑦ 漏掉的地方：** (a) `definition_math` 的 `statement` 一直無條件 static，三個 deck 共 13 場自己寫了 `{show statement}` 的場其實零幀差；(b) `scaffold.*`（motive／problem／flag）同樣無條件 static，於是 motive 在 t=0 就劇透、`ASSUMES` pill 比旁白早一分鐘出現（R2 D-focus）——集中修在 `templates/__init__._scaffold_reveal_timing`，每個 template 一體適用、版面不變。 **兩支新 hook（R2 點名最重的兩場）：** 場 04 beat 4 先寫旁白真正描述的中間式再 `ReplacementTransform` 成定稿（兩個 2 在畫面上消掉，關 R2 D-carry must＋R1b L-why must），beat 5 的 result 列改建成三段 MathTex、每個因子在旁白念到它時才到（關 R2 D-focus）；場 03 把多項式差商放進本場一直空著的右半並把 h 消掉，後面已有的 `math.0`（三角差商→0/0）於是讀得出它是對照組（關四鏡共四條）。 **B-3 accent 複審（使用者裁決）：** 查證 `chapter3.tex` 第 30–207 行（§3.1）**一個 `envdefinition` 都沒有**（第一個在 §3.2），而影片有 7 場標 `accent: definition`＝definition 還等於中性藍時留下的舊帳。散文／Figure 場（03/06/11/17/24/25）改為**不標 accent**；derivation 場（04/07）改標新的 `accent: derivation`→result 藍（依據 `calcbook.sty:96` 把 `aResult` 註為「theorem/proposition/proof/**主色**」）。順手修掉 Direction B 寫死 accent 的最後一處（derivation 的 result 註解 tag 原為恆 `amber_ink`）。**dividers 的 `accent: definition` 刻意未動**（幕級家具、無可對位的講義環境，待裁決）。 **B-4 場 20 `companion_limit` 重排：** 移出第四幕、改放第二幕末（`radians_essential` 之後）——四鏡認證它在原位是孤兒（開的是「apply」幕但它是一個極限、後面無人回用、首句指四場之前）；移位後首句指三場前，且 `all_six_tan_sec` 變成它本來就讀起來像的第四幕開場。純文字搬移、旁白未動。 **事故＋新護欄（使用者裁決固化）：** 為量測 5 場而跑 `tts.py` 時**漏了 `--reuse-existing`**——`tts.py:1054` 的 `use_reuse = args.reuse_existing and …` 才會建 reuse index，沒有它 index 是空的、`scene_reuse_ok` 根本不會被問到，於是 5 場 hash 逐字未變的場全部重合成、其中一場再掉到 beats 終端（`--fallback-budget` 只管 rungs 2–3），共 **13 次 billed call**；離線 preflight 直接呼叫 `scene_reuse_ok`、繞過了這道 CLI 閘，所以預測與實際相反。**後果：03/04/06/08/20 五場的 take 換新**（字句相同、QA 全 clean、`prob_mean` 0.85–0.89 vs 原版 0.88），原檔已覆寫。補救花 1 次：帶 `--max-billed-calls 1` 重取 take，QA 探針判 fail（離線診斷確認是**誤報**——ASR 把逐字母唸的 `O A B` 聽成 `OAB`，`qa_diff` 讀成 3-token replace），上限在第 2 次呼叫前中止、省下 beats 終端的 8 次；扶正 `.tmp` ＋ `--reuse-existing --skip-qa --no-billing` ⇒ `backend_calls: 0`，場 06 回到 `scene_aligned`。**新旗標 `--no-billing`／`--max-billed-calls N`：上限涵蓋整個 run 含 beats 終端**，超過的那一次呼叫在打出去之前中止；WAV 要等閘全過才 promote，所以中止時 manifest 與音檔原封不動、可安全重試。`RUNBOOK`／`KICKOFF`／docstring 一併改成查證後的版本。 **量測注意（新事實）：** `rewatch_pack` 把畫面縮成 192×108 灰階再比對，0.2% 門檻 ≈ 400 px，而一個字形在那個尺度只有 2×3 px——**「隨旁白書寫」對 0.2% 門檻近乎隱形**（場 03 報 34.2／09 報 41.2／18 報 22.8），同一段在 0.05% 門檻是 8.5／10.8／8.2、靜止比例 0.59/0.74/0.70。已逐幀確認是書寫動畫在跑。**建議文字動畫場以 0.05% 為判準，兩個門檻都列。** **驗收：** `run_selftests` 31→**32 全綠**（新增 `_selftest_pacing` 17 支，含 mutation check）；`doctor --smoke` 與開工時逐字相同；`schema` structure OK、`sizecheck` 0 error；`derive_spoken --check` parity OK。 **交付物＝[`content_scripts/_audit/REVIEW-ch03_s31-amplify-applied.html`](content_scripts/_audit/REVIEW-ch03_s31-amplify-applied.html)**（驗收表＋逐場改動＋畫面前後對照＋事故報告）。**獨立稽核：** 視覺幀稽核（21 張最終幀）開 **1 blocking**——場 18 `derivative_cycle` 的 `d/dx` 約 10 px／行（<26 px font floor 的一半）、用全幀最暗的 muted 墨，而它是畫面上**唯一**說明「箭頭＝一次微分」的元素；根因是它被縮了三次（`	frac` × label 尺寸 × 環的 fit clamp），而 clamp 吃緊是因為環被夾在窄縫裡、幀下方 1.3u 卻空著。修法＝`rac`＋text 墨、`d/dx` 移到環心（不再佔高度預算）、`math.1` 下移進死區把縫拉開、節點升 `math_sm`、垂直箭頭 0.22u→0.78u。**23.6 px → 81 px，環高 0.99u → 1.81u。回歸稽核 0 blocking、未引入新問題**；回歸另開的兩條耐久性問題（`d/dx` 仍是環內最小墨、環高只差 0.02u 就會再觸發 clamp）也在同一輪處理（`d/dx` 移出被 clamp 量測的群組，headroom 0.02u→0.12u）。其餘 6 條 advisory 未動。 **R2 導演鏡 Opus 5 盲審重跑（只讀 pack）：must 5→2、should 26→20、verdict ↑10 =14 ↓3（bad 0），基線那 5 條 must 全部關閉**（正是本輪與 ⑦⑫ 動過的場）；評審主動點名場 04 的新手法「應該推廣到 07、09、14、21、22」。**DoD 第 2 項未達成**——新增 2 條 must，都是「這裡該有圖」的內容提案（場 09 全片最抽象的一場無圖、場 12 擋誤解卻無畫面支撐），各需新 hook，**未自行動手**。 **另修 `rewatch_pack.py` 改為雙門檻上報**：pack 只報 0.2% 門檻，而一個字形在 192×108 下只有 2×3 px，於是 R2 據此寫下「場 09 連續 41.2 秒不動」「全片動態約 9%」——同一支影片在 0.05% 門檻是 10.8 s 與 **16.0%**。現在兩個門檻都列，INDEX 明寫「兩者不一致時相信細的、去看幀」。 **狀態＝等使用者看片裁決是否進 Task D（計費）。**

- **產線評估修復輪（2026-09-12；依 [`_audit/REVIEW-pipeline-assessment-2026-09-07.html`](_audit/REVIEW-pipeline-assessment-2026-09-07.html) 的 F1–F12，回歸審核附於該 HTML 末節）：** **① 級已修**——F1 `provenance.py` 路徑漂移（legacy standalone）→ 改雙來源解析（凍結 legacy standalone ∪ `.tex` calcbook label key：`doc:sec:3.1`／`thm:`／`fig:`…），正典 deck 過閘、`_selftest_pedagogy` fixture 改 hermetic；F2 機制＝內容稿標頭 `source_rev` 講義源 stamp（`pipeline/source_rev.py`，warn-only）掛進 schema／make／derive，§3.1／§3.2 已回補 lock 時 stamp → **兩稿現在持續 `[source_rev]` WARN＝講義已漂移，§8 對齊（diff→外科修改→scoped NFA→重 TTS 計費）待併入 §3.1 重做試點／§3.2 解凍，未做**。**② 級已補**——F4 `pipeline/run_selftests.py`（22 selftest 單一 runner）＋`tools/doctor.py --smoke`（正典 deck schema／lint／derive --check）；F5 文檔雙軌收尾（METHODOLOGY §5/§6/§8、README、REVIEW_GATES、PEDAGOGY／SIXLENS／AMP rubric、pedagogy／AMP agent）；F6 `scratch_{isolate,mathtex,split}.py` → `_archive/scratch/`、`capacity_selftest.py` → `pipeline/_selftest_capacity.py`（`scratch_frames.py` 為現役 QA 工具，留在原位）。**--smoke 順帶抓到報告未列的一條：** `schema.py` 的 SC 查 `.md` 沒剝 `_mimo` → 正典 `_mimo` deck 在 SC 10 ERROR（make.py 不跑 SC 故 render 未受影響）；統一走 `provenance.content_script_for` 修掉。**待你裁決（未動）：** F3 AMP 錨——存量 472 筆 `<!-- expansion -->` 是否回填 `.tex`（rubric／agent 已先改成「`.tex` 為 0 筆時退回凍結 fragment 當池、報告明寫池來源」，不再靜默空跑）；③ 級 F7–F12 結構性（R2 水位決定／R4 motion primitive／R5 靜默 beat／R6 lesson arc／R7 兩級製作／R8 停點瘦身）全屬設計裁決，本輪不動。
- **[完成] 產線硬化 T1–T11**（kickoff [`KICKOFF-pipeline-hardening.md`](KICKOFF-pipeline-hardening.md)，分支 `video/pipeline-hardening`，**已併入 main 2026-07-19**）：**Phase 0＋1 完成（2026-07-11）**。Phase 0：分支自 `84c2799`、綠基線 selftest 全綠。Phase 1（T1–T4，7 commits `0d6954e`→`320871f`）：T1 `_mimo.yml` freshness gate（`derived_check.py` 雙輸入 LF-norm stamp）；T2 tts.py 計費面（`--backend` 必填＋preflight fail-closed guard／dry-run planned-worst 報表／`--scene` 併回 prior／計費收據）；T3 `--unit auto` allowlist 單一源（`template_names.py`＋parity）；T4 ASR QA verdict 三態落 manifest。17 selftest 全綠、demo mock 整合驗收過。**Codex 對抗 review（Terra/max）usage-limited 中斷**，浮現 1 edge（null-char audio_file str 過 type check 但 `build_reuse_index` `Path()` crash）＝評估非 blocking（自產 manifest 不可能、fail-safe pre-synth、Karpathy §2；詳 scratch `codex-phase1-assessment.md`）。**§3.2 進真 TTS 前必做硬化＝達成**。**T1–T10 全落地（15 commits `0d6954e`→`fc5baae`，21 selftest 全綠）：** T5 `{show}` cross-check／T6 listening pack／T7 timeline+vtt+chapters（真 render 端到端驗過）／T8 單次編碼+bt709+faststart（真 render 驗 h264/yuv420p/bt709/aac48k）／T9 per-boundary 轉場+`--output`／T10 loudness two-pass loudnorm。**T9、T10 為裁決件——A/B 交付物已產、標 `awaiting adjudication`，等使用者裁決：** T9＝`video/_audit/REVIEW-transition-ab.html`（content-content 邊界該淡黑還是硬切）；T10＝`video/_audit/REVIEW-ch03_s31-loudness-ab.html`（house 響度 target -19/-17/-15.5 LUFS）。**Codex 對抗 review 已完成（gpt-5.6-terra/max）：T1–T10 複核找 3 條 blocking（make.py mock fail-open 蓋真 Dean WAV／derived_check 沒強制雙輸入契約／loudness_ab CLI ModuleNotFoundError＋假綠），已修（commit `f8c91ca`）並跑回歸再審確認「3 條已關閉、0 新 blocking」。** 21 selftest 全綠、真 render 整合驗過。**使用者 2026-07-11 裁決：轉場 A（content-content 也淡黑＝現行預設）／house loudness -19 LUFS／null-char 不硬化——T9-4（記文檔）＋T10-3（-19 loudnorm 接進 compose，commit `4ed34f0`）＋T11（7 檔文檔統一收尾：DESIGN 新契約/旗標/loudness/轉場、beats-not-free 更正、REVIEW_GATES/README/RUNBOOK/ENVIRONMENT/doctor stale 行）已落地。** **產線硬化 T1–T11 完成、Codex-clean、§3.2 可進真 TTS。** 下一步＝使用者發起 §3.2 spoken→derive→NFA→真 TTS→render，或 §3.1 整節重做（新產線全流程驗收）。**（→ 2026-07-19 定案：§3.1 整節重做＝品質補強試點、§3.2 凍結，見下品質補強輪。）**
- **品質補強輪（2026-07-19 grilling 定案；詳錄見 [archive](_archive/REBUILD_LOG-2026-05-to-07.md) 同日節）：** 錨＝品質補強（非降本）；§3.1 成片視覺＋教學兩面不滿、尚無具體場景清單。執行序：**① 看片標記**（standalone HTML 逐場標記表；成片仍在本機 `output/ch03/s3.1/`）→ **② 定視覺水位＋擬「重設計授權」範圍**（骨架可重排／可加鋪墊提問／詳略重分配；數學仍逐單元 source-bound、無習題外洩）→ **③ §3.1 整節重做**（試點，舊片＝對照組；VISUAL-FRAME detection 驗證＋成本表「同內容兩制度」對比行順帶落地）→ **④ 新舊 A/B 裁決 → 教義定案 → §3.2 解凍**。忠實教義＝逐節實驗、不全域改寫；聽感不在本輪（FA 降級 3 場維持「先不用」）。各步間停等使用者發動；目前狀態＝**等使用者發動 ①**。
- §3.2 MiMo 路線（spoken → NFA → render）——**凍結至品質補強試點 ④ 裁決**（2026-07-19，見上品質補強輪）。
- VISUAL-FRAME detection 面驗證（併入下一個重跑節）。（原並列的 `{show}` target-vs-payload 交叉驗證已由硬化 T5 落地於 sizecheck，2026-07-11。）
- 真 4K final render（§3.1）另議。
- spoken.yml 白名單 auto-prefill（V4 工程 backlog）——等下方成本量測顯示「手寫 spoken」是主要成本再啟動；產物仍過 parity＋NFA。
- 未消費產物（house audio A/C＋caution ping、孤兒 ch01_inverse_trig hooks、derive_spoken ch01 殘留）＝2026-07-07 清理（見該日 commit）。

- **品質補強輪 ⑱＝派工制首輪：色軸兩次裁決＋鋪滿輪 backlog（2026-09-13；kickoff [`KICKOFF-round17-dispatch.md`](KICKOFF-round17-dispatch.md)；另一 session 的 Task D 記在 ⑰）。** **制度（使用者裁決，寫進根 `CLAUDE.md`「任務分派」）：** 主模型（Fable 5.1）只做拍板／決策／審核／難題；需要判斷力或品質的派 **Opus**（hook 編舞、各鏡稽核、跨模組設計、卡住的除錯；拿不準就派 Opus），契約寫死的機械改動派 Sonnet，文字整理派 Haiku；有檔案改動一律 worktree、一 task 一 commit、主對話只 merge＋審核（開分支後先 `git merge main`——這輪三個 worktree 從舊 commit 開出來、落後十幾個 commit）；獨立項目用 `spawn_task` 開新對話（本輪開了 `worked_example` 模板的 chip）。**裁決一：色軸照 11／18／24——藍＝cos、琥珀＝sin、綠＝斜率／加速度／tan；06／07／08 對齊（Sonnet，`c5dd432`）。裁決二：θ 物件與 token 改 `strategy` 紫**——琥珀與赭色相只差幾度，06 的內三角／扇形、08 的半弦／弧疊在一起分不出來，幀證實後使用者選「θ 換色」而非「明度／紋理分辨」；`meta.color_map`、06 扇形、08 弧、07 θ 段一起換（Sonnet，`5fd41c8`）；§3.1 沒有 procedure 場，紫色原本空著。**backlog 落地（各一個 Sonnet 子代理）：** prose 行內 θ 吃色表——在 `$…$` 內對命中 token 注入 dvisvgm `\special{color push/pop}`，manim 對 SVG 內非黑字形本來就保留顏色，所以零後處理（`24536f9`；鋪滿輪六幀 advisory 一次消掉）；theorem_proof 的 proof dict 列收 `seg_roles`＋schema 擴掃（`44ed099`）；場 23 `shm_device` hook——彈簧與重物隨 `s=sin t` 擺、速度箭頭藍、加速度箭頭綠永遠指向平衡線、位移琥珀（R2 must；`ff8c3d1`），順帶拿掉 23 的 `focus indicate step.0`（hook 填滿拍後閃爍被擠到拍尾與 exit 疊、sync 飄 +0.8 s）。**另一 session 同時：** Task D 對齊講義後 04／09／23 三場超過 12 s 線——09 由它修（proof.1 變形展開到拍長，15.2→10.0 s，`3621597`）；04 的 step.0 拍旁白 39.7 s、靜止 25.8 s，派 **Opus** 做「u／v 展開臨時草稿」覆蓋層 hook——cue 取自 forced alignment 逐字時間、隨旁白走，代換→展開→兩個 sin u cos v 框選消去→存活段對位飛回 step.0→拍尾清空，**fine 最長靜止 25.8→7.5 s**（`e308588`）；23 靠彈簧 hook 填。**工具教訓（另一 session 抓到，已固化 `e2a29de`）：** `rewatch_pack` 只列 coarse events，而 coarse 看不見字形書寫，會把 fine 死區指到錯的拍（09 指到 proof.0，實為 proof.1）——現在每場 md 直接列 fine 的 change events 與最長靜止的起訖秒數＋所在拍，INDEX 明寫「定位死區看 fine」。**閘：** 五個 merge 後 main `run_selftests` 40/40、schema OK、sizecheck 0 error／1 既有 warn、smoke 與 ⑯ 基線只差 source_rev WARN 消失（⑰ 把 stamp 換到 chapter3.tex）。**合併版驗收（另一 session render，main `55d5a42`）：** 全片 17.5 分；**21 場 fine 最長靜止第一次全部 ≤ 12 s**（最高 17／24 各 11.5、22 10.8；04 25.8→7.5、23 12.8→4.2、09 15.2→10.0），`[stillness]` 15 條 advisory 型態一致＝Task D 拉長的拍子「一次淡入然後 hold」（16／17 proof 列、recap point.0–2、19 statement＋math.0；用 `paced:` 可修）。視覺幀稽核（Opus）：⑯ 的 08 blocking 與六幀 prose θ advisory 全部消失；**新 1 blocking＝09 `continuity_template` hook 收尾 gap→0 後 x 與 x₀ 標籤永久重疊**（另一 session 認領），advisory 9（含 V10：07 result／10 proof／11 圖同一組量三場色不對位——裁決＝07 result 與 10 proof 列改函數色 cos 藍／sinθ⁄θ 琥珀／1 muted，與 11 的圖對位；07 步驟列保留面積來源色）。R2 Opus 盲審 27 場：**good 7／ok 13／weak 7／bad 0**，must 2（06 偶函數插圖疊舊單位圓鬼影、09 half-sum 因子未標記——都在另一 session 的 hook，已轉它）、should 23，`by_rule` ML4 11／ML2 6／ML5 4／ML1 4／ML3 2；weak 六場＝Task D 拉長拍子的 hold。報告 [`REVIEW-ch03_s31-round18-dispatch.html`](content_scripts/_audit/REVIEW-ch03_s31-round18-dispatch.html)；成片 `output/ch03/s3.1/ch03_trig_derivatives_mimo__round18.mp4`。**第二次合併（main `638995d`→`fc5e872`，另一 session render）：** 07 result／10 proof 改函數色（`21d7a0c`）、五條 advisory 小修（`9c74807`：07 carry 0.62、24 carry 0.6、23 裝置標籤 math_sm、11 游標 x_to 0.25、17 π 刻度 buff）、另一 session 四件（09 x／x₀ 標籤重疊、09 half-sum 框選＋共用 |·|≤1、06 evenness 底板不透明＋聯集遮蔽、12 y 軸上緣 1.32）＋兩個機制修正（`focus.apply` 壓暗改乘法式 `fade`——原本把鏤空徽章環填實；06 的 `focus` 從未生效改由 hook 自己壓暗）。21 場 fine 最長靜止仍全 ≤ 12 s（最差 17／24 各 11.5）。視覺回歸（Opus）：21 張場末幀 **blocking 0**（⑱ 的 09 已關；07 result／10 proof／11 圖 token 色完全對位），新 advisory 8（17 π 刻度改被導引虛線穿過、07 來源列的 1/cos θ 綠 vs result 藍、07 角落圓 13–16 px、12 箭頭淨空 8 px、14 reason 欄兩種字級…）；補充幀那 6 條因 `critic_after19/` 被另一 session 重跑覆寫而補審中。R2 Opus 盲審：good 6／ok 13／weak 8／bad 0，must 5／should 18，`by_rule` ML4 15／ML2 7／ML1 4／ML3 1／ML5 1——五條 must 全是 Task D 拉長旁白後的畫面缺口：03 sine 那行沒演出「失敗」、12 讀數晚一拍、13 度數 caution 全場無圖、17 proof.0 口述推導 11.5 s 無動作、24「Watch where」那拍標記晚 11.5 s；R2 總評「主症狀不是沒動，是動得比旁白晚一拍」。**工具教訓：** `critic.py` 輸出路徑寫死、跨輪累積、兩 session 互蓋——已派 Sonnet 加 `--out`＋修場號撞號；exit 場 `[sync]` 偏長的根因＝hooks 老 helper `_draw`／`_peel`／`_fade` 回傳寫死秒數（`scene._tail` 契約成立，A/B render 差 0.0003 s），另一 session 派 Opus 全檔遷成 `_elapsed` 量測值。**⑲ 之後合進 main 的四件機制修正（2026-09-13 下午）：** ① hooks 的 callable 一律回報 renderer 實際耗時（`_spent`；另一 session 的 Opus，`09cbb9e`）——`[sync]` 8→6 條，`_fade`／`_draw` 原本多報 0.10 s 與底層低報互相抵銷，拆開後 06 的數字反而變差（+0.657→+0.790）但誠實；**前提＝`make.py` 的 `disable_caching: True`（快取開著時 `renderer.time` 是標稱值，`_elapsed` 會無聲退化）**，已寫進 DESIGN 並在旗標旁加註；殘餘在 `pipeline/`（`pacing.walk`／`play_block` 標稱值、每拍 wait 整幀進位）與 `ch03_chain_rule_hooks.py` 的同款 `_fade`，另開一件。② `critic.py --out`＋抽幀場號改讀 storyboard 序（`2cc14e6`）——固定輸出路徑跨輪累積、兩 session 互蓋，且 manifest 的 `scene_number` 在 `--scene` 子集併回時沿用舊值（17 撞號）。③ **tts reuse key 改 `scene_id＋text_hash`（Opus，`0dc1c3b`＋`dd722ee`）**——beat 與 scene 兩層的 WAV 路徑都嵌場號／reveal，搬場或加 marker 就命不中而重合成計費（Task D 白花 5 次、⑯ 的 24 要手動改檔名）；現在命中時自動搬檔登記、合併時場號依 storyboard 序重算；正典 deck 離線驗證 `backend_calls: 0`、dry-run 反映真實成本（beats 模式的場一律 `--unit beat`，`--unit auto` 會路由去 scene-level 計費 1 次）。④ **focus 還原失效（06 ineq 拍 ①② 不還原）根因＝順序**（Sonnet，`7e375bb`）：`_play_content` 每拍先 `play_block` 再套 focus，paced 的 reveal 鋪滿整拍，`dim: []` 的還原被擠到拍尾——真 manim 最小重現否證了「乘法 fade 疊加」與「物件脫鉤」兩個假說；修法＝純還原 entry 在該拍 reveal 之前跑。**狀態＝⑳ 進行中：Opus ×3 做 R2 must（03 sine 演出失敗、13 度數塌平圖、17 proof.0 草稿），另一 session 做 12／24（hook 內定時）＋收斂 9 處裸 `_elapsed`；合併後 render 第 20 輪 → 視覺回歸＋R2。**

- **品質補強輪 ⑰＝Task D（對齊講義＋兩處外科重排）＋R2 兩條新 must＋六條視覺 advisory＋divider accent（2026-09-13；使用者裁決「四件都同意，一次都做完」；kickoff [`KICKOFF-s31-amplify.md`](KICKOFF-s31-amplify.md) §6 已勾選）。** 交付物＝[`content_scripts/_audit/REVIEW-ch03_s31-taskd-applied.html`](content_scripts/_audit/REVIEW-ch03_s31-taskd-applied.html)＋成片 `output/ch03/s3.1/ch03_trig_derivatives_mimo.mp4`（17.5 分）＋pack `rewatch_pack_after17/`。 **Task D-2（§8 對齊）：** 21 個 content 場逐單元對 `handout/latex/src/ch03/chapter3.tex` §3.1 diff，**5 場跟改**（03 補和差化積恆等式的來源／06 補 corner piece $ABC$ 一句／17 補伴隨恆等式的來源／23 收回過強的「exactly the functions」——$s''=-s$ 的解空間是 $a\sin+b\cos$／26 接收從 13 移來的度數導數公式）、16 場判定不跟改；`source_rev` stamp 換到 `.tex`（`sha256:acc1feed…`），**常駐的 `[source_rev] WARN` 消失**。 **Task D-1（兩處外科重排，刻意與講義次序不同，理由記在 content script 表頭）：** 06 偶函數論證移到不等式之後（19 秒離題原本卡在「這是新工具」與主圖之間，四鏡同指；現在是最後一拍、改成覆蓋層，`focus:` 把完成的圖壓暗、hook 用 ground 色墊板 z=9／內容 z=10 蓋住）、13 度數導數公式移到 recap（原位置在場 16 證出 `sin'=cos` 之前，是前向參照）。 **R2 兩條新 must：** 場 09 `continuity_template`（proof.1 由 proof.0 `TransformMatchingShapes` 過去＝同一模板填兩次、螢幕文字一字未改；右下角半距數線在 qed 那拍把 $x$ 走進 $x_0$——數線**建在 qed 的 reveal 內部不另立 marker**，因為該場在 beats 路線上、加 marker 要 3 次計費）、場 12 `ratio_readouts`（建在另一階段的 `carry:` 之上：角落 inset 在「At a fixed angle」那拍 `ReplacementTransform` 成延伸到 $\theta=\pi$ 的全幅圖——場 11 是 $x\in[-1.85,1.85]$ 的近景、$\pi$ 在畫面外——再逐拍落 $2/\pi\approx0.64$ 與 $0$ 兩個讀數）。 **六條 advisory：** 五條做掉（場 18 標籤相交 9→0 並加 `_selftest_figure_labels.py` 當回歸閘／場 04・14 的 rail 改成「場級右移到最寬那列放得下 `MIN_LEADER=0.55u`」、淨空 30→111 px 與 16→97 px／場 06 右側三個 glyph 改疊層讓徽章 1:1 成立、版面 parity 逐位相同／`graph` 新增 `x_label`／`y_label` 覆寫、場 11 軸標改 θ／場 18 cos 的讀數點與曲線一起升 secondary，三張圖用同一種方式說 cos），第六條「全節字級」只做了 graph 放大與一個 tag，**整體字級交裁決**（見下）。另收同伴 session 轉來的兩條（場 18 `y=\cos x` 的 label_point、場 19 節點色條改「只說是哪個函數」）。四個 divider 拿掉 `accent: definition`。 **NFA 兩輪：** 第一輪 **4 blocking 全部在花錢之前抓到**，其中三條同一類——新寫的句子沒套本片既有的去歧義慣例（`A plus B over two` 會被聽成 $A+\frac B2$、`sine of u plus v` 會被聽成 $(\sin u)+v$），**漏了就會被合成進付費音檔**；兩條念法慣例已寫進 [`NARRATION-FAITHFULNESS-RUBRIC.md`](content_scripts/_audit/NARRATION-FAITHFULNESS-RUBRIC.md) 權威表與 `derive_spoken.py` 摘錄表。回歸輪 **0 blocking**、確認沒有改過頭或漏檔。 **TTS 帳＝15 次 billed call（核准 12＋10＝22），9 次浪費在兩個坑，兩次都被 `--max-billed-calls` 擋住、舊音檔零污染**（教訓已寫進 [`RUNBOOK-mimo-narration-route.md`](RUNBOOK-mimo-narration-route.md)）：① **QA 探針不是只對「逐字母唸讀」誤報**——凡 spell out 的數學都會被 ASR 轉回符號而判 misspeak ⇒ 場判 fail ⇒ 重合成；只給場 06 下 `--skip-qa` 時四場跑下來三場重試、7 次呼叫一場都沒 promote，六場全下之後 **5 場 5 次、0 retry**。**scene-level 重合成一律 `--skip-qa`。** ② **beat 級 reuse 以輸出檔路徑為 key，而場號與 reveal 都在檔名裡**——B-4 搬場之後 `derivative_of_cosine` 由 16 變 17，整場 6 個 beat 路徑全失配，改 1 個 beat 的字要重合成 6 個；把 beats 目錄複製到新場號、manifest 指過去之後回到 `backend_calls: 1`。全片旁白 895.8 → **963.6 s**。 **驗收（0.05% 細門檻）：** **Task D 把五個場推過 12 s 線**——成因單一：對齊講義讓那些拍子長了 6–35 秒而畫面仍只有一次 reveal。三個修掉（06 20.8→**8.8**、26 20.2→**10.5**，兩者都是原語 7；12 12.5→**6.8**，θ=π 的讀數改成沿曲線走過去——那一場是我自己新寫的 hook 沒填滿最後一拍），**三個沒修、交裁決**（04 **25.8 s**／09 **15.2 s**／23 **12.8 s**）：場 04 的畫面已經滿了（四列算式 y+1.26→−3.27，加不了第五列），真正的解是照 NFA 建議做一塊覆蓋層寫出 u／v 的展開，屬新 hook、本輪不自行擴張範圍。其餘閘：`run_selftests` **40/40**、`schema` structure OK、`sizecheck` 0 error／1 既有 warn、標籤相交量測六場全 0。 **三件回歸稽核抓到的自製問題（都已修）：** 偶函數覆蓋層墊板 z=0 沉在圖底下（抽幀才看出來）、新加的「螢幕支撐」tag 寬到把場 04 剛修好的 leader 又弄不見、字級量測沒套 TeX preamble（`\everymath{\displaystyle}` 讓堆疊分數在小行裡仍以 display 尺寸排版）導致六個 tag 改寫有五個沒有證據、已回歸。**交裁決：** ① 04／09／23 三場的新畫面要不要做（不需重 TTS）② `prose_sm`／`tag` 整體字級（會動到每個 deck，與 kickoff §7.1「取消 `math_sm 40`」併案）③ 場 04 現為全片最長（108 秒）要不要縮。

## 每節成本量測（2026-07-07 起；量三節後檢視模板紅利，再定 per-scene 客製 hook 上限）

| 節 | audit/撰稿 tokens | render 次數 | 客製 hook 數 | 真 TTS calls | 回歸輪數 |
|---|---|---|---|---|---|
| §3.1（基線，事後估） | 單景打磨 8 項×80–216k | 多輪（Step 0／capacity v2 回歸含 48 場） | 8 | ~36（21 場＋fallback＋重合成） | 3+ |
| §3.2 | 待記 | 待記 | 待記 | 待記 | 待記 |
