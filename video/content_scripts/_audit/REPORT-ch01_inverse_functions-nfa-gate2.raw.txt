VERDICT: 0 blocking, 1 advisory

- [Advisory] [D3] shape_can_mislead — source `$h(x)=x(x-2)(x+2)$` is spoken as “h of x equals x times the quantity x minus two times the quantity x plus two”; adjacent grouped factors would benefit from an audible pause after “minus two” → Verdict: Rewrite → `h of x equals x times the quantity x minus two, times the quantity x plus two`

Tier 3: none.

D1 clean  
D2 clean  
D4 clean  
D5 clean  
D6 clean  
D7 clean

## Convention recommendations (D5)
Keep current conventions: `$f^{-1}$` → “f inverse”; subscripts → “x sub one/two”; coordinates → “the point with coordinates…”; grouped roots/fractions use “the quantity” / “all over” where needed. For adjacent binomial factors, prefer comma-separated factor pauses as above.

## TTS sanity (D6)
Clean. `mimo-v2.5-tts`, voice `Mia`, `wav` 24kHz/mono/PCM16, user-message style instruction, and no audio tags are internally consistent.

## Math-content check (D7)
Clean. I recomputed the stated examples and checks: one-to-one tests, factorization, inverse formulas, composition identities, domain pairings, and Celsius/Fahrenheit inverse all match.