meta: { id: _fixture_sc, section: "0.0", chapter: "Chapter 3" }
scenes:
  - id: s_missing
    kind: content
    template: theorem_proof
    ref: md:proved_here
    covers: [def]          # `reduced` (recap_required) left uncovered -> SC2 warn
    say: "{show statement}"
    statement: "partial proof (the recap of the reduced form is left uncovered)"
