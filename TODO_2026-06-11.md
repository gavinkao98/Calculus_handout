# TODO — 2026-06-11（換機交接）

> 今天在公司機台的工作全部已 commit 並 push 到 `origin/experiment/seed-converge`。
> 回家第一步見「換機 checklist」。本檔是一次性交接筆記，事項完成後可刪。

## 今天定案了什麼（題目擴充策略）

**問題**：HTML 講義題目太少、種類不豐、不全面（老師講稿先天簡略）；先前「AI 自行出題＋人工審核」會向原型題收斂。

**定案的新流程**（已寫進 [`CONTENT_EXERCISES.md`](CONTENT_EXERCISES.md) §Sourcing workflow，commit `556de96`）：

1. **手稿題必收**——老師手稿裡的題目一定要有，作為每節覆蓋矩陣的種子。
2. **開放題庫補缺**——初版講義寫完後，對每節建「覆蓋矩陣」（列＝該節教過的每個
   definition／theorem／strategy；欄＝conceptual／computational／reasoning／applied
   四類），手稿題填格，**空格＝購物清單**，再去本地題庫篩候選給人工審核。
   題庫的 taxonomy 只當搜尋索引，**缺口永遠由講義自身內容定義**（防 scope creep）。
3. **AI 出題退為備援**——只填題庫填不了的格（如緊扣手稿 running example 的延伸題）。

**配套修訂**（回應「每節 8–12 題＋固定配比寫死缺彈性」）：

- 8–12 題降為**預設校準帶**；四類配比降為**全書預設先驗**。
- **權威判準改為覆蓋矩陣**：漏練到的教學點＝違規、湊數的近重複題＝違規；
  純題數出 band 不再是違規。配比審計移到**章層級**（節層級依內容傾斜）。

**授權定調**（講義永遠免費公開，不商用）：

- 成品掛 **CC BY-NC-SA 4.0** ＋ credits 頁 → BY／BY-NC／BY-NC-SA 家族題庫全部可合法 remix。
- 逐題出處標記三分類：`[source: 題庫…]`／`[source: AI]`／手稿題。
- **紅線**：不收 CC BY-SA（如 Active Calculus，與 NC-SA remix 不相容）、
  「免費瀏覽但保留版權」來源（如 Paul's Online Math Notes）、College Board AP 歷屆題。
- WeBWorK OPL（最初的候選）定位收窄為「taxonomy 掃缺口＋變式量產」，
  排在 CLP／OpenStax／18.01SC 之後；PG/Perl 抽取管線等基本盤補完再評估。

## 今天 push 上去的 commits

| commit | 內容 |
|---|---|
| `556de96` | CONTENT_EXERCISES.md 改制＋§Sourcing workflow；`problem_banks/` 落地（README＋gitignore）；docs lint 排除第三方題庫 |
| `6930a63` | handout_kit standalone 化收尾：舊範本版 HTML（chapter{2,3}-{screen,print}、poc-*）刪除入版控；RESULT-s42 斷鏈修正；`book_docs_lint` 歸零 |
| `e7b41bc` | video 線交接快照：三新靜態模板（value_table／graph_compare／sign_chart）＋graph_focus `reveal: true`＋三模板列距修繕＋字體定案同步。**demo 成片待認可** |

## 換機 checklist（家裡電腦）

1. `git pull`（分支 `experiment/seed-converge`）。
2. **重抓題庫**（內容 gitignored、不隨 git 走）——照 [`problem_banks/README.md`](problem_banks/README.md)
   的三行 `git clone --depth 1` 指令（CLP-1／Mooculus／APEX v5）。
3. video 線若要跑：`.venv`＋ffmpeg 環境見 `video/README.md` §環境（vendored `.deps*` 同樣不隨 git 走）。

## 接下來要做（優先序）

1. **Ch 1 pilot（最高優先，基礎設施已就緒）**：
   - 對 Ch 1 每節產覆蓋矩陣 → 標手稿題已覆蓋的格 → 空格清單。
   - 從 CLP-1 篩候選（`problem_banks/CLP1/latex/problembook/problems/prob_sN.M.tex`，
     每題 question／hint／answer／solution 同檔；CLP-1 Ch1 是 limits，反函數部分
     可能要靠 Mooculus／APEX 補）→ 候選清單給使用者審核。
   - 通過後改寫入庫：Stewart register、house notation（CONTENT_SPEC §9）、
     `[source:]` 註記；同時把官方解答存進該章旁的 `exercise-imports.md`（原料，非答案附錄）。
   - pilot 要驗證的：矩陣粒度合不合手感、候選量多寡、審核流程順不順。
2. pilot 跑完定型：`exercise-imports.md` 的格式；HTML kit
   [`CONTRACT-html-writing.md`](experiments/handout_kit/CONTRACT-html-writing.md) 的
   `env-exercise` 段擴充（題號、出處、將來的難度標記在 HTML 怎麼呈現）。
3. 視缺口接入更多題庫（候選表已在 [`problem_banks/README.md`](problem_banks/README.md)：
   CLP-2、OpenStax、MIT 18.01SC、UBC MER、OPL）。
4. 講義全域授權聲明＋credits 頁落地（放哪、怎麼寫）。
5. （既有 deferred，非本輪）full exercise design round：答案附錄格式、難度標記、
   type 標記語法——等多數章節主內容完成後開（CONTENT_SPEC §14）。
6. （video 線，與題目線無關）看三模板 demo 成片＋4 段 §1.1 動畫並認可 →
   之後才是旁白 TTS（**計費，屆時先報價徵同意**）→ VLM critic → 4K 定版。

## 開放問題（pilot 時順便決定）

- 覆蓋矩陣本身要不要進版控？（傾向：要，放章節旁如 `coverage-matrix.md`，審核紀錄即文檔。）
- Ch 1 的 exercise 補進哪個檔？（ch1 內容現狀是 designer 原樣 `example-ch01/`＋
  standalone 產物，動手前先確認入口與 `gen_standalone.py` 的銜接。）
- HTML 講義題目區塊現在只有 `env-exercise` chip，編號／出處／難度的視覺呈現
  等 pilot 餵了真實需求再定。
