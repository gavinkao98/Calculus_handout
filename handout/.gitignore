# Regenerable render artifacts (screenshots). The shot.mjs tool itself is tracked.
_render/*.png

# Headless Chrome scratch profiles (normally in %TEMP%, ignored here just in case)
hk-chrome-*/

# Audit reference figures (binary; only the text prompts/results are tracked)
**/_audit/*.png
