% appendixB.tex — appB 全篇組裝驅動（KICKOFF-latex-pilot.md M-B3 交付物 2）
%
% body 由 convert.py 確定性轉換產出（fragment 唯讀；數學逐位元組 pass-through）：
%   cd handout/tex_export && python convert.py appB --out build/appendixB-body.tex
% 編譯（CWD 必須在 template/——vendored Inter 的 fontspec Path 綁定於此）：
%   cd handout/tex_export/template && latexmk -lualatex -output-directory=../build/appB appendixB.tex
\documentclass[a4paper,12pt,oneside]{memoir}
\usepackage{calcbook}

\begin{document}
\input{../build/appendixB-body.tex}
\end{document}
