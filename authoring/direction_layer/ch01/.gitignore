# Regenerable build / audit artifacts — rebuild from the .tex / re-run the audit.
# Audit prompts are assembled from audit_preamble_*.txt + rules.md + seed + brief + draft.
audit_prompt_*.txt
# Preview build: PDF + page renders are regenerable from cmp_*.tex (see RESULT_ch01.md).
*.pdf
preview_*.png
# LaTeX intermediates.
*.aux
*.log
*.out
*.toc
*.idx
*.ilg
*.ind
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.bcf
*.run.xml
