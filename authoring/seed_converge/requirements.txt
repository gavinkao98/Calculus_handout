# Direct deps of the authoring/seed_converge R&D loop (figure_critic.py / run.py).
# Networking is stdlib urllib; API keys come from the environment (GEMINI_API_KEY /
# OPENAI_API_KEY / DEEPSEEK_API_KEY) — never committed. The only third-party dep is:
pymupdf==1.27.2.3   # `import fitz` — in-process PDF rasterization for figure critique
#
# These are folded into the repo-wide ../../requirements.lock; installing that lock
# (tools/setup.ps1) provisions this too. See ../../ENVIRONMENT.md.
